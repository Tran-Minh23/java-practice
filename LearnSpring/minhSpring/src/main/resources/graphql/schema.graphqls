type Query {
  # bookById(id: ID): Book
  # authorById(id: ID): Author
  # recentPosts(count: Int, offset: Int): [Post]!
}

type Book {
  id: ID
  name: String
  pageCount: Int
  author: Author
}

type Author {
  id: ID
  firstName: String
  lastName: String
}

enum Episode {
  NEWHOPE
  EMPIRE
  JEDI
}
