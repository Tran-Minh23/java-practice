{
    "java.configuration.updateBuildConfiguration": "automatic",

    // Let VS Code's Java extension treat `TestStuff/` as a source root so
    // "Run" / "Debug" CodeLens works for `TestStuff/Test.java`.
    "java.project.sourcePaths": ["TestStuff"],
    "java.project.outputPath": "bin"
}
